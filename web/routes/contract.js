/**
 * Contract Routes — /api/contract/* route handlers
 * Placeholder for on-chain contract interaction endpoints.
 * The registry ABI and contract helpers are available via lib/config and lib/wallet.
 */

module.exports = function (routes) {
  // No contract-specific routes defined in the original server.
  // Future endpoints for ServiceRegistry interaction can be added here.
};
